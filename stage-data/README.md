Stage data exports copied from `index.html`.

- Total maps: 11
- Stage files are numbered in gameplay order.
- Each JSON includes `sequence`, `exportName`, and `displayNameNumbered`.
- `index.json` lists every exported stage file.

These files are reference/storage exports only. The runtime still reads stage data from `index.html`.
